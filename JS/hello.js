// camelcase
// 변수
let dog
let variable
// 객체
const userInfo = {
    name: 'Tom',
    age: 20
}
// 함수
function add() {}
function getName() {}
//pascalcase
// 클래스
class User {
    constructor(option) {
        this.name = option.name
    }
}
// 생성자 함수
function User(options) {
    this.name = options.name
}
// SNAKE_CASE
// 값이 바뀌지 않을 상수
const API_KEY = 'my_key'
const PI = Math.PI

// 재할당이 일어나지 않는 변수
const NUMBERS = [1, 2, 3]